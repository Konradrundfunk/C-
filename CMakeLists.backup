cmake_minimum_required (VERSION 3.5)

projekt (HelloWorld)


set (source_dir "${PROJEKT_SOURCE_DIR}/src/")
set (CMAKE_CXX_FLAGS"$ {CMAKE_XSS_FLAGS} -Wall -Werror -std=c++14")

file(GLOB source-files "${source_dir}/*.cpp")

add_executable (HelloWorld ${source_files})
